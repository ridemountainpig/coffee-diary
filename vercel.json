{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "installCommand": "ENV PUPPETEER_CACHE_DIR=/src/.cache/puppeteer && pnpm install",
    "buildCommand": "pnpm run build"
}
